/**
 * UNPRO — useQuoteAnalysis Hook
 * TanStack Query hook for quote analysis operations.
 *
 * Future: useQuery for fetching analyses, useMutation for uploads.
 */

export const useQuoteAnalysis = () => {
  return { quotes: [], isLoading: false };
};
