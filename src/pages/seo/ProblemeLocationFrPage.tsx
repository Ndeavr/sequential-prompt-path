/**
 * UNPRO — Problem+Location French route (/probleme/:problem/:city)
 * Re-exports the existing ProblemLocationPage.
 */
import ProblemLocationPage from "./ProblemLocationPage";
export default ProblemLocationPage;
