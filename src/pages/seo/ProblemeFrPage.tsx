/**
 * UNPRO — Problem SEO Page (French route /probleme/:slug)
 * Re-exports the existing ProblemPage with French canonical URL.
 */
import ProblemPage from "./ProblemPage";
export default ProblemPage;
