/**
 * UNPRO — AIPP Score Service
 * Handles API calls for AIPP scoring.
 *
 * Future methods:
 * - getAIPPScore(entityId, entityType)
 * - getScoreComponents(entityId)
 * - getScoreHistory(entityId)
 * - recalculateScore(entityId)
 */

export const aippScoreService = {
  // Placeholder — will connect to Supabase + AI
};
