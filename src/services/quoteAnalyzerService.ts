/**
 * UNPRO — Quote Analyzer Service
 * Handles all API calls related to quote analysis.
 * Uses TanStack Query for caching and synchronization.
 *
 * Future methods:
 * - uploadQuote(file) → uploads quote document
 * - analyzeQuote(quoteId) → triggers AI analysis
 * - getQuoteAnalysis(quoteId) → fetches analysis results
 * - getQuoteHistory(propertyId) → fetches all quotes for a property
 * - compareQuotes(quoteIds[]) → compares multiple quotes
 */

export const quoteAnalyzerService = {
  // Placeholder — will connect to Supabase + AI edge functions
};
