/**
 * UNPRO — Home Score Service
 * Handles API calls for property condition scoring.
 *
 * Future methods:
 * - getHomeScore(propertyId)
 * - getScoreBreakdown(propertyId)
 * - getScoreTrend(propertyId, dateRange)
 * - getRecommendations(propertyId)
 */

export const homeScoreService = {
  // Placeholder — will connect to Supabase
};
