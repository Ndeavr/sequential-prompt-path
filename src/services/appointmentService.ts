/**
 * UNPRO — Appointment Service
 * Handles scheduling and appointment management.
 *
 * Future methods:
 * - getAvailableSlots(contractorId, date)
 * - bookAppointment(data)
 * - cancelAppointment(appointmentId)
 * - getAppointments(userId)
 * - syncWithGoogleCalendar(userId)
 */

export const appointmentService = {
  // Placeholder — will connect to Supabase + Google Calendar
};
